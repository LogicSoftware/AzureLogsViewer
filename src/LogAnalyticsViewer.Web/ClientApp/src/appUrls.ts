const root = process.env.PUBLIC_URL;

export const appUrls = {
    search: `${root}/api/search`,
    list: `${root}/api/queries/list`,
    get: `${root}/api/queries/get`,
    update: `${root}/api/queries/update`,
    create: `${root}/api/queries/create`
};
